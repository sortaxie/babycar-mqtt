spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
      profile: dev
      label: master

eureka:
  client:
    serviceUrl:
      defaultZone: http://adorgroup:123456@www.ie-goal.cn:8761/eureka/